# Plan

- [x] Reuse the confirmed `nvrc-local-source` baseline contract instead of rebuilding the old upstream line.
- [x] Complete idea-stage literature and route selection for the downstream pipeline question.
- [x] Submit a durable idea line for a frozen-upstream, machine-facing downstream pipeline.
- [ ] Freeze the inherited upstream module: identify checkpoint, config, and bounded data subset.
- [ ] Define the first machine-facing export schema:
  - reconstructed video
  - aligned metadata
  - optional teacher/tokenizer side outputs when already available
- [ ] Build one minimal end-to-end downstream pipeline with a frozen consumer model.
- [ ] Run the first comparison package:
  - original video input
  - reconstructed video input
  - optional compressed-side interface input if the export path is already stable
- [ ] Record the first main experiment and write the route decision for whether feature-packet or larger-model integration is justified next.
