# Status

Baseline reuse is already resolved: `nvrc-local-source` remains the accepted
upstream asset.

Current stage: `idea`

Current judgment:
- The relation repair is now a completed negative result, not the next route.
- The locked localization package still gives the decisive failure boundary:
  target-side packets remain discriminative while exported predicted packets are
  already collapsed, and the earliest accessible collapse surface is
  `exported_predicted_packets_or_earlier`.
- The active code path exposes `pred_feat`, `target_feat`, `pred_delta`, and
  `target_delta`, but still does not expose `query_projection` or
  `query_packet_head`.
- The fresh anti-collapse literature sweep favors explicit predicted-side
  variance control over another teacher-side repair family.
- The current serious frontier is intentionally narrow:
  1. Recommended: localization-first plus an explicit variance-floor repair on
     accessible predicted packet surfaces.
  2. Second best: add a stronger decorrelation / redundancy-reduction term if
     variance-only repair looks too weak.
  3. Fallback: reopen interface redesign if the bounded predicted-side repair is
     still negative or if deeper query-side surfaces remain inaccessible.
- Reasons the recommended route wins:
  - two measured teacher-side repair families already failed
  - the current target branch is already fixed and asymmetric, so another
    asymmetry-only trick is not the best first transfer from the SSL literature
  - the active failure mode is low-diversity predicted-side packet structure on
    a fixed evaluation surface

Deferred:
- another teacher-side auxiliary-loss repair
- broader packet-interface redesign before one bounded predicted-side repair is measured
- deeper multimodal / VLM integration before the packet bridge is trustworthy
- reopening codec-line ranking

Next durable action:
- revise the active anti-collapse idea package with the refreshed literature and
  candidate ranking
- hand off to `experiment` for one bounded predicted-side
  localization-plus-repair package on the same frozen widened surface
