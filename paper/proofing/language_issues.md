# Language Issues

## Blocking Issues

- None.

## Minor Notes

- The oracle row is now explicitly described as a diagnostic upper bound rather than a deployable method.
- The draft consistently uses `bounded`, `frozen`, and `non-learned` language to protect scope.
- If a later submission package is prepared, run one more light copy-edit pass for venue-specific style only.
