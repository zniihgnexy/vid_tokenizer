# Revision Log

## Issue R1

- Why it matters: the paper line is now scientifically supportable, citation-backed, and package-complete at the markdown-first level, but the runtime still has not durably registered the bundle delivery state.
- Fix type: writing-plus-bundle
- Blocks finalize: no
- What should change:
  - Keep the manuscript content fixed and either register the bundle durably or record the runtime-side blocker explicitly.
- Practical checklist:
  - keep the manuscript at the current two-claim boundary
  - do not reopen experiments or widen claims
  - resolve the runtime-side bundle-registration step if possible; otherwise carry the blocker into closure notes

## Resolved In This Pass

- The title/story mismatch from the previous draft is resolved.
- The claim-evidence map is now explicit.
- The draft now clarifies that the current arbitration rule is a simple non-learned heuristic rather than an unspecified broad adapter family.
- The draft now includes a citation-backed positioning paragraph and a bibliography surface.
- A first proofing report and language-issues note now exist for the current bounded paper line.
- A markdown-first bundle manifest now records the current handoff-ready paper package.
- The broader three-bundle validation is now mapped explicitly as a claim-boundary note instead of being left implied outside the paper package.
- The draft now clarifies that the oracle row is diagnostic only, not a deployable route.
- The earlier chunked 16-frame QB-Norm revalidation is now mapped into the active paper contract as appendix-level robustness evidence instead of floating only in the analysis folder.

## Recommended Next Route

1. Treat the current markdown-first package as the active internal paper bundle.
2. Retry durable bundle registration only if the runtime state now permits it; otherwise record the blocker explicitly and proceed with honest closure bookkeeping.
3. Add LaTeX/PDF build artifacts only if a venue-formatted submission package becomes necessary.

## Experiment Route

No mandatory new experiment is required for the current narrow claim.
